# WBS — 이음(Eum) 여행 동선 플래너

> **작성 기준**: 1인 프리랜서(기획·개발·퍼블리싱·디자인 겸임) / Claude Code(Opus 5) 페어 개발
> **최종 갱신**: 2026-08-25
> **저장소**: https://github.com/you4ranghe/eum.git

---

## 0. 이 WBS를 읽는 법

혼자 하는 프로젝트의 WBS는 **공수 산정표가 아니라 "다음에 뭘 할지 잊지 않기 위한 장치"**다.
그래서 일반적인 WBS와 다르게 세 가지를 명시한다.

| 표기 | 의미 |
|---|---|
| **차단** | 내 코드로 못 푸는 것. 계정·키·승인이 필요. **먼저 처리하지 않으면 뒤가 다 막힌다** |
| **검증법** | 이 항목이 "끝났다"고 말할 수 있는 구체적 조건. 이게 없으면 진척이 착시가 된다 |
| **역할** | 기획(P) / 개발(D) / 퍼블리싱(F) / 디자인(X) — 혼자 하더라도 모드를 바꿔야 판단이 안 흐려진다 |

**공수는 시간이 아니라 "세션 수"로 적는다.** 1세션 ≈ 집중 2~3시간.
1인 프로젝트에서 시간 단위 추정은 거의 항상 틀리지만, 세션 단위는 대체로 맞는다.

---

## 1. 마일스톤

| # | 마일스톤 | 정의 (= 이게 되면 끝) | 누적 세션 |
|---|---|---|---|
| **M0** | 기반 구축 | 저장소·문서·CI가 서고, 목 데이터로 전체 흐름이 돈다 | 3 |
| **M1** | 지도가 뜬다 | 실제 API 키로 네이버/구글 지도가 렌더링되고 탭 전환된다 | 6 |
| **M2** | 데이터가 남는다 | 로그인 후 일정을 저장하고 새로고침해도 유지된다 | 11 |
| **M3** | 동선이 계산된다 | 검색→담기→순서최적화→동선계산이 실제 API로 동작한다 | 16 |
| **M4** | 알림이 온다 | 브라우저를 닫아도 출발 알림이 온다 | 20 |
| **M5** | 배포 & 다듬기 | 프로덕션 URL에서 낯선 사람이 써도 막히지 않는다 | 25 |

---

## 2. Phase 0 — 기반 구축 (M0)

> 이미 완료된 항목이 많다. 이번 세션의 실제 작업은 W0-4 ~ W0-6.

### W0-1. 프로젝트 스캐폴딩 ✅
- [x] Next.js 16 + TS strict + Tailwind 구성
- [x] 디렉토리 구조 확정 (`domain` / `geo` / `map` / `providers` / `routing` 분리)
- **역할**: D · **검증법**: `npm run build` 통과 ✅

### W0-2. 지도 제공자 추상화 ✅
- [x] `MapAdapter` 인터페이스
- [x] `NaverMapAdapter` / `GoogleMapAdapter`
- [x] `registry.ts` — 지역별 가용 제공자 판정
- **역할**: D · **검증법**: 타입 체크 통과 ✅ / 실제 렌더링은 **W1-3에서 검증**

### W0-3. DB 스키마 설계 ✅
- [x] `0001_init.sql` — 테이블 + RLS
- [x] `0002_functions.sql` — 재정렬 RPC + 알람 스케줄러
- **역할**: D · **검증법**: `supabase db push` 성공 → **W2-1에서 검증**

### W0-4. 저장소 연결 및 문서화 ⬅ **현재**
- [ ] `git init` + 원격 연결
- [ ] `CLAUDE.md` — 에이전트 작업 규약
- [ ] `wbs.md` — 이 문서
- [ ] 초기 커밋 & 푸시
- **역할**: P · **공수**: 0.5세션
- **검증법**: GitHub에서 `main` 브랜치 파일이 보인다

### W0-5. 개발 환경 검증
- [ ] `npm run dev` 기동 확인
- [ ] 목 데이터로 날짜 탭 → 일정 목록 렌더링 확인 (지도는 키 없어 회색)
- [ ] 콘솔 에러 0건
- **역할**: D · **공수**: 0.5세션
- **검증법**: `localhost:3000`에서 3일차 탭 전환이 동작하고, 체류시간 입력 시 도착 시각이 재계산된다

### W0-6. CI 최소 구성
- [ ] GitHub Actions — push 시 `typecheck` + `build`
- **역할**: D · **공수**: 0.5세션
- **검증법**: 일부러 타입 에러를 넣고 푸시했을 때 Actions가 빨간불
- **왜 지금**: 혼자 하면 로컬 빌드를 건너뛰는 날이 반드시 온다. CI는 그날의 보험이다.

---

## 3. Phase 1 — 지도가 뜬다 (M1)

### 🔴 W1-0. API 키 발급 — **차단**
- [ ] 네이버 클라우드 플랫폼 가입 → **Maps (Web Dynamic Map)** 이용 신청 → Key ID 발급
- [ ] NCP **Directions 5** 이용 신청 (동선 계산용, W3에서 사용)
- [ ] NCP **Search(Local)** 또는 네이버 개발자센터 검색 API 발급
- [ ] Google Cloud 프로젝트 생성 → **Maps JavaScript API / Places API (New) / Routes API** 활성화
- [ ] 두 콘솔 모두 **일일 쿼터 상한** 설정 ← 이거 안 하면 유출 시 요금 폭탄
- [ ] 브라우저 키는 `localhost:3000` + 배포 도메인으로 **HTTP 리퍼러 제한**
- [ ] 서버 키는 리퍼러가 아니라 **API 제한**만 (서버에서 리퍼러를 못 보냄)
- **역할**: P · **공수**: 1세션 (승인 대기 시간 별도)
- **⚠️ 사용자가 직접 해야 함.** 이게 안 되면 W1-1 이후 전부 대기.

### W1-1. 환경변수 주입
- [ ] `.env.local` 작성
- [ ] 키 누락 시 지도 영역에 **안내 문구**가 뜨는지 확인 (이미 구현됨)
- **역할**: D · **공수**: 0.2세션 · **선행**: W1-0

### W1-2. 네이버 지도 렌더링 검증
- [ ] 제주 중심으로 지도가 뜬다
- [ ] 목 일정의 마커 3개가 번호와 함께 보인다
- [ ] 마커 간 점선(직선 폴백)이 그려진다
- **역할**: D · **공수**: 0.5세션
- **검증법**: 브라우저 스크린샷. 콘솔 에러 0건
- **예상 함정**: NCP 신규 키는 `ncpKeyId` 파라미터(구 `ncpClientId` 아님). 이미 반영했으나 콘솔 확인 필요

### W1-3. 구글 지도 렌더링 + 탭 전환 검증
- [ ] 탭 클릭 시 지도가 교체되고 **이전 마커가 남지 않는다** ← 어댑터 `destroy()` 검증 포인트
- [ ] 전환 후에도 보고 있던 위치/줌이 유지된다
- [ ] 지역을 '오사카'로 바꾸면 네이버 탭이 비활성 + 사유 툴팁이 뜬다
- **역할**: D · **공수**: 0.5세션
- **검증법**: 네이버↔구글 5회 왕복 후 유령 마커 0개, 메모리 증가 없음

### W1-4. 반응형 레이아웃 마감
- [ ] 데스크톱: 사이드바 400px 고정 + 접기 토글
- [ ] 모바일: 바텀시트 (지도 인스턴스가 재생성되지 않는지 확인)
- [ ] 태블릿 세로에서 깨지지 않는지
- **역할**: F/X · **공수**: 1세션
- **검증법**: 375 / 768 / 1440 폭에서 가로 스크롤 0

---

## 4. Phase 2 — 데이터가 남는다 (M2)

### 🔴 W2-0. Supabase 프로젝트 생성 — **차단**
- [ ] supabase.com 프로젝트 생성 (리전: **Northeast Asia (Seoul)** — 지연 시간 직결)
- [ ] `npx supabase login && link --project-ref <REF>`
- [ ] `npx supabase db push` — 마이그레이션 적용
- [ ] `npm run db:types` — 타입 생성
- **역할**: D · **공수**: 0.5세션 · **⚠️ 브라우저 로그인 필요, 사용자 직접**

### W2-1. 인증 (이메일 매직링크)
- [ ] 로그인 페이지 `/login`
- [ ] `middleware.ts` — 세션 갱신 (서버 컴포넌트는 쿠키 쓰기가 막혀 있어 미들웨어가 필수)
- [ ] 미로그인 시 목 데이터 + "로그인하면 저장됩니다" 배너
- **역할**: D/F · **공수**: 1.5세션
- **검증법**: 로그인 → 새로고침 → 세션 유지
- **설계 판단**: 매직링크 채택. 비밀번호 방식은 재설정·변경 화면 2개가 추가로 필요한데,
  1인 프로젝트에서 그 2개는 다른 기능 하나와 맞바꾸는 비용이다.

### W2-2. 여행 CRUD
- [ ] 여행 목록 / 생성 (제목·지역·기간)
- [ ] 생성 시 `trip_day` 자동 생성 (2박3일 → 탭 3개)
- [ ] 여행 삭제
- **역할**: D · **공수**: 1.5세션
- **검증법**: 2박3일 생성 → 날짜 탭 3개 → 다른 계정으로 로그인 시 **안 보임** (RLS 검증)

### W2-3. 일정 저장 연동
- [ ] `useTripStore` → Supabase 동기화 (낙관적 갱신 + 디바운스 저장)
- [ ] 장소 담기 시 `place` upsert → 진짜 UUID 발급
- [ ] 새로고침해도 일정 유지
- **역할**: D · **공수**: 2세션
- **검증법**: 장소 3개 담고 새로고침 → 순서·시간 그대로
- **주의**: 목 데이터 폴백을 **제거하지 말 것**. 오프라인/장애 시 화면이 죽는 것보다 낫다.

### W2-4. RLS 침투 테스트
- [ ] 다른 사용자의 `trip_id`로 직접 조회 시도 → 빈 결과
- [ ] anon 키로 `trip` 테이블 전체 조회 시도 → 본인 것만
- [ ] `service_role` 키가 클라이언트 번들에 없는지 확인 (`grep`)
- **역할**: D · **공수**: 0.5세션
- **왜 별도 항목인가**: RLS는 조용히 실패한다. "동작한다"와 "안전하다"는 다르고,
  이건 배포 후에 발견하면 이미 늦다.

---

## 5. Phase 3 — 동선이 계산된다 (M3)

### W3-1. 장소 검색 연동
- [ ] 네이버 Local Search 실호출
- [ ] 검색 결과 → 지도에 임시 마커
- [ ] 결과 클릭 → 지도 이동 + 상세 모달
- **역할**: D · **공수**: 1세션
- **검증법**: "성산일출봉" 검색 시 좌표가 맞는 위치에 찍힌다
- **알려진 한계**: 네이버 Local Search는 **영업시간을 주지 않는다** → W3-2로 이어짐

### W3-2. 영업시간 보강
- [ ] 구글 Places Details로 영업시간 보강 (네이버 결과에 병합)
- [ ] 보강 실패 시 "영업시간 정보 없음" 표기 (이미 구현됨)
- [ ] 보강 결과를 `place` 테이블에 캐시 → 재호출 방지
- **역할**: D · **공수**: 1세션
- **비용 주의**: Places Details는 필드 등급별 과금. `regularOpeningHours`만 요청할 것.

### W3-3. 동선 계산
- [ ] "동선 계산" 버튼 → 실제 Directions 호출
- [ ] 폴리라인이 도로를 따라 그려진다 (직선 점선 → 실선)
- [ ] `route_leg_cache` 히트 확인 (2회차는 API 호출 0)
- [ ] 네이버 실패 → 구글 폴백 → 직선 추정 폴백 동작 확인
- **역할**: D · **공수**: 1.5세션
- **검증법**: 같은 일정 2회 계산 시 두 번째는 네트워크 탭에 외부 호출 없음

### W3-4. 순서 최적화 UI
- [ ] "순서 최적화" 결과를 **미리보기**로 먼저 보여주고 확인받기
- [ ] 되돌리기
- **역할**: D/X · **공수**: 1세션
- **설계 판단**: 즉시 적용하지 않는다. 사용자가 의도적으로 짠 순서를
  알고리즘이 말없이 뒤집으면 신뢰를 잃는다. 최적화는 제안이지 명령이 아니다.

### W3-5. 드래그 앤 드롭 재정렬
- [ ] `@dnd-kit/core` 도입 (react-beautiful-dnd는 유지보수 중단)
- [ ] `reorder_trip_stops` RPC 연동 (이미 작성됨)
- [ ] 재정렬 시 해당 날짜 경로 캐시 무효화 (이미 구현됨)
- **역할**: D/F · **공수**: 1.5세션
- **검증법**: 순서 변경 → 지도 마커 번호가 즉시 따라간다

### W3-6. `routing/` 단위 테스트
- [ ] `haversine` — 알려진 두 지점 거리 검증
- [ ] `optimizeOrder` — 고정 지점이 실제로 고정되는지
- [ ] `getOpenStatus` — 자정 넘김("22:00"~"02:00") 케이스
- **역할**: D · **공수**: 1세션
- **왜 여기만 테스트하는가**: 순수 함수라 테스트 비용이 가장 낮고,
  버그가 나면 **조용히 틀린 값**을 내놓아 눈으로 못 잡는 유일한 영역이다.

---

## 6. Phase 4 — 알림이 온다 (M4)

### W4-1. 브라우저 내 알람 검증
- [ ] 알림 권한 요청 흐름
- [ ] 출발 10분 전 알림 발화
- [ ] 탭 백그라운드 후 복귀 시 밀린 알람 회수 (`visibilitychange`)
- **역할**: D · **공수**: 0.5세션
- **검증법**: 시스템 시각을 조작하거나 `fireAt`을 1분 뒤로 넣고 실제 알림 확인

### W4-2. Web Push 인프라
- [ ] VAPID 키 생성
- [ ] Service Worker (`public/sw.js`) — push 수신
- [ ] `push_subscription` 저장
- **역할**: D · **공수**: 1.5세션
- **차단 없음** (VAPID는 자체 생성)

### W4-3. 서버 알람 디스패처
- [ ] Supabase Edge Function `dispatch-alarms`
- [ ] `pg_cron` 1분 스케줄 등록 (`0002_functions.sql` 주석 블록)
- [ ] 일정 변경 시 `replace_day_alarms` RPC 호출
- **역할**: D · **공수**: 1.5세션
- **검증법**: **브라우저를 완전히 닫고** 알림이 오는지 ← 이게 진짜 검증
- **왜 Vercel Cron이 아닌가**: Hobby는 하루 1회. "출발 10분 전"이 성립하지 않는다.

### W4-4. 알람 소음 제어
- [ ] 5분 넘게 밀린 알람은 발송 안 함 (뷰에 이미 반영)
- [ ] 같은 지점 중복 알림 방지
- **역할**: P/D · **공수**: 0.5세션
- **왜**: 여행 앱 알림은 한 번만 짜증나도 사용자가 권한을 영구 차단한다. 회복 불가.

---

## 7. Phase 5 — 배포 & 다듬기 (M5)

### W5-1. Vercel 배포
- [ ] `npm i -g vercel` → `vercel link`
- [ ] 환경변수 등록 (Production / Preview 분리)
- [ ] 지도 API 키 리퍼러에 **배포 도메인 추가** ← 잊으면 배포 후 지도만 안 뜬다
- **역할**: D · **공수**: 1세션

### W5-2. 온보딩 흐름
- [ ] 첫 방문 시 "제주 2박3일 샘플로 시작하기"
- [ ] 빈 상태(empty state) 문구 정리
- **역할**: P/X · **공수**: 1세션
- **왜**: 지도 앱의 빈 화면은 사용자가 뭘 해야 할지 모르는 대표적 이탈 지점이다.

### W5-3. 디자인 마감
- [ ] 색·간격 토큰 정리 (현재 Tailwind 확장 6개 → 검토)
- [ ] 로딩/에러 상태 일관성
- [ ] 접근성: 키보드 내비게이션, 대비비 4.5:1, 색 단독 의존 제거 (배지는 이미 텍스트 병기)
- **역할**: X/F · **공수**: 1.5세션

### W5-4. 성능
- [ ] 지도 SDK를 사이드바 렌더 이후 로드 (LCP 개선)
- [ ] Lighthouse 모바일 ≥ 85
- **역할**: D · **공수**: 1세션

### W5-5. 운영 준비
- [ ] Supabase 무료 티어 **7일 무활동 일시정지** 대응 (주 1회 헬스체크 크론)
- [ ] 에러 로깅 (Vercel 기본 로그로 시작, 부족하면 그때 도입)
- [ ] `README` 최종 갱신
- **역할**: P/D · **공수**: 0.5세션

---

## 8. 백로그 (M5 이후 — 지금은 하지 않는다)

의도적으로 미룬 것들. **"나중에"가 아니라 "이 조건이 되면"**을 적어둔다.

| 항목 | 착수 조건 |
|---|---|
| 일정 공유 링크 | 사용자가 2명 이상 생겼을 때 |
| 여러 명이 같이 편집 | 공유 링크가 실제로 쓰일 때 |
| 숙소 기준 자동 동선 | 사용자가 숙소를 실제로 등록하기 시작할 때 |
| 예산 관리 | 별도 앱을 쓰는지 먼저 관찰 |
| PWA 설치 | 재방문율이 30% 넘을 때 |
| 지역 확장 (오사카/도쿄) | 제주 일정이 100건 넘을 때. 코드는 이미 준비됨(`regions.ts`) |
| 다국어 | 해외 지역 확장 이후 |

---

## 9. 리스크 등록부

| 리스크 | 영향 | 대응 | 상태 |
|---|---|---|---|
| 지도 API 쿼터 소진 | 서비스 중단 | 캐시 + 수동 트리거 + 일일 상한 | 대응 설계 완료 |
| 네이버 영업시간 미제공 | 핵심 기능 반쪽 | 구글 Places로 보강 (W3-2) | 대응 예정 |
| Supabase 7일 일시정지 | 데모 당일 장애 | 주 1회 헬스체크 (W5-5) | 미착수 |
| Vercel Hobby 상업 사용 불가 | 수익화 시 위반 | 수익화 시점에 Pro 전환 | 인지 |
| 1인 프로젝트 번아웃 | 프로젝트 중단 | 마일스톤 단위로 **동작하는 결과물** 확보 | 설계에 반영 |

---

## 10. 진척 요약

| Phase | 완료 | 전체 |
|---|---|---|
| P0 기반 구축 | 3 | 6 |
| P1 지도 | 0 | 5 |
| P2 데이터 | 0 | 5 |
| P3 동선 | 0 | 6 |
| P4 알림 | 0 | 4 |
| P5 배포 | 0 | 5 |
| **합계** | **3** | **31** |
