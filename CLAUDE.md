# CLAUDE.md — 이음(Eum) 작업 규약

> 이 문서는 Claude Code(Opus 5)가 이 저장소에서 작업할 때 매 세션 읽는 규약이다.
> 사람이 읽는 문서가 아니라 **에이전트가 따르는 계약**이므로, 취향이 아니라 판단 기준을 적는다.

---

## 0. 이 프로젝트에서 나의 역할

20년차 프리랜서로서 기획·개발·퍼블리싱·디자인을 혼자 겸한다.
따라서 **한 사람이 유지보수 가능한 복잡도**가 모든 결정의 상한선이다.

| 역할 | 이 프로젝트에서의 판단 기준 |
|---|---|
| 기획 | 기능을 늘리기 전에 "이게 없으면 여행을 못 짜는가"를 먼저 묻는다 |
| 개발 | 추상화는 **두 번째 구현이 나타난 뒤에** 만든다. 단, 지도 제공자는 요구사항이 처음부터 2개라 예외 |
| 퍼블리싱 | 디자인 시스템 대신 Tailwind 토큰 6개로 버틴다. 컴포넌트 라이브러리 도입은 40개 화면부터 |
| 디자인 | 정보 밀도 > 장식. 지도 앱은 콘텐츠가 지도이므로 UI는 물러나야 한다 |

**혼자 하는 프로젝트에서 가장 비싼 것은 코드가 아니라 "3개월 뒤의 내가 이해 못 하는 코드"다.**
그래서 이 저장소는 주석 밀도가 평균보다 높다. 특히 **왜 그렇게 했는지**를 남긴다.

---

## 1. 기술 스택 (확정, 변경 시 WBS 갱신 필수)

```
Next.js 16 (App Router) + TypeScript strict
  ├─ 상태: Zustand (Context/Redux 금지 — 지도라는 비-React 세계와 붙일 땐 과함)
  ├─ 스타일: Tailwind CSS (CSS-in-JS 금지 — 런타임 비용)
  ├─ DB/인증: Supabase (Postgres + RLS + Auth)
  ├─ 지도: Naver Map / Google Maps (MapAdapter로 추상화)
  └─ 배포: Vercel Hobby (무료)
```

### 제약 조건 (타협 불가)

- **Java 17 사용 불가** → Spring Boot 계열 재도입 금지. 서버 로직은 Route Handler 또는 Postgres 함수.
- **Vercel Hobby / Supabase Free** → 유료 기능을 전제하는 설계 금지. 대안이 없으면 WBS에 제약으로 명시.
- **Vercel Hobby Cron은 하루 1회** → 분 단위 스케줄은 Supabase `pg_cron`으로만.
- **Supabase Free는 7일 무활동 시 프로젝트 일시정지** → 데모 전 반드시 깨워둘 것.

---

## 2. 코드 작성 규칙

### 2.1 절대 규칙

1. **도메인 타입에 SDK 타입을 넣지 않는다.** `src/lib/domain/types.ts`는 순수해야 한다.
   `naver.maps.*` / `google.maps.*`는 각 어댑터 파일 밖으로 나올 수 없다.
2. **`any`는 어댑터 내부에서만 허용한다.** 네이버 지도는 공식 타입이 없어 불가피하지만,
   그 대가로 앱의 나머지가 타입 안전을 유지한다. 다른 곳의 `any`는 리뷰에서 반려.
3. **서버 경유 여부의 유일한 기준은 "비밀 키가 필요한가"다.**
   RLS로 충분한 것을 Route Handler로 감싸지 않는다. 무료 티어에서 함수 호출은 자원이다.
4. **외부 API를 자동으로 호출하지 않는다.** 사용자 명시 액션(버튼)에만 호출한다.
   쿼터 소진은 곧 서비스 중단이고, 무료 티어에서는 복구 수단이 없다.
5. **`SUPABASE_SERVICE_ROLE_KEY`는 공용 캐시 테이블 쓰기에만 쓴다.**
   사용자 데이터에 쓰는 순간 RLS가 무의미해진다.

### 2.2 주석 규칙

```ts
// ❌ 무엇을 하는지 (코드가 이미 말한다)
// 마커를 렌더링한다
adapter.renderMarkers(markers);

// ✅ 왜 그렇게 했는지
// 매번 새로 만들지 않고 위치/아이콘만 갱신 → 날짜 탭 전환 시 깜빡임 방지
```

**"왜"가 없는 코드는 3개월 뒤 리팩터링 대상이 아니라 지뢰가 된다.**
비자명한 판단(폴백 전략, 성능 트레이드오프, 외부 API 특성)에는 반드시 근거를 남긴다.

### 2.3 파일 배치

| 위치 | 무엇이 들어가는가 |
|---|---|
| `src/lib/domain/` | 순수 타입. 의존성 0 |
| `src/lib/geo/` | 지역 설정. **확장 지점** — 새 도시는 여기 객체 추가만 |
| `src/lib/map/` | 지도 렌더링 추상화 (클라이언트) |
| `src/lib/providers/` | 외부 데이터 API (서버 전용, 키 사용) |
| `src/lib/routing/` | 거리·시간·최적화. **순수 함수** — 서버/클라이언트 양쪽 실행 가능해야 함 |
| `src/app/api/` | 비밀 키가 필요한 것만 |
| `src/components/` | UI. 비즈니스 로직 금지 |
| `src/store/` | 상태만. 부수효과 핸들(어댑터 인스턴스 등) 금지 |

---

## 3. Git 규약

### 브랜치

```
main            ← 배포 가능 상태만. 직접 커밋 금지(단, WBS P0 초기 세팅 제외)
└─ feat/<wbs-id>-<slug>   예: feat/w2-1-supabase-auth
└─ fix/<slug>
└─ chore/<slug>
```

### 커밋 메시지

Conventional Commits + **WBS ID를 본문에 남긴다.**
WBS와 커밋이 연결되지 않으면, 혼자 하는 프로젝트에서 진척도를 잃는다.

```
feat(auth): Supabase 이메일 로그인 연동

WBS: W2-1
- 매직링크 방식 채택 (비밀번호 재설정 UI를 만들지 않아도 됨 = 화면 2개 절약)
- middleware에서 세션 갱신 — 서버 컴포넌트는 쿠키 쓰기가 막혀 있음

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
```

**type**: `feat` `fix` `refactor` `chore` `docs` `style` `perf` `test`

### 커밋 단위

- **WBS 항목 하나 = 커밋 하나**를 기본으로 한다.
- 빌드가 깨진 상태로 커밋하지 않는다. `npm run typecheck && npm run build`가 통과해야 한다.
- 커밋/푸시는 사용자 승인 없이 진행한다 (이 프로젝트 한정, 사용자가 위임함).

---

## 4. 작업 순서 (매 세션)

```
1. wbs.md 를 읽고 다음 미완료 항목을 확인한다
2. 브랜치를 만든다 (main 직접 작업 금지)
3. 구현한다
4. npm run typecheck && npm run build 로 검증한다
5. wbs.md 의 해당 항목 체크박스를 갱신한다
6. 커밋 + 푸시
7. 검증하지 못한 것을 사용자에게 명시적으로 보고한다
```

### 보고 규칙

**"됐습니다"는 검증한 것에만 쓴다.** 다음은 반드시 구분해서 보고한다.

- ✅ 검증함 — 빌드 통과 / 실제 실행 확인
- ⚠️ 미검증 — 코드는 작성했으나 API 키·계정이 없어 실행 못 함
- ❌ 미착수 — 차단 요인과 함께

허위 진척 보고는 혼자 하는 프로젝트에서 **가장 비싼 실수**다.
나중에 데모 직전에 터진다.

---

## 5. 이 프로젝트에서 하지 말 것

| 하지 말 것 | 이유 |
|---|---|
| 컴포넌트 라이브러리 도입 (MUI, Chakra) | 지도 앱은 커스텀 UI 비중이 높아 이득보다 싸움이 크다 |
| 상태관리 라이브러리 교체 | Zustand로 충분. 바꾸는 순간 전 컴포넌트가 영향받는다 |
| 테스트 프레임워크 전면 도입 | 혼자 하는 MVP 단계. 단, `src/lib/routing/`은 순수 함수라 **테스트 가치가 높으니 예외** |
| ORM 추가 (Prisma, Drizzle) | Supabase 클라이언트로 충분. 타입은 `db:types`로 생성 |
| 마이크로프론트엔드 / 모노레포 | 1인 프로젝트에 조직 구조용 도구를 넣지 않는다 |
| `iframe`으로 네이버/구글 지도 상세 임베드 | `X-Frame-Options`로 차단된다. 조용히 실패한다 |
| 지도 SDK를 `useEffect` 없이 초기화 | SSR에서 `window`가 없어 터진다 |

---

## 6. 자주 쓰는 명령

```bash
npm run dev            # 개발 서버
npm run typecheck      # tsc --noEmit
npm run build          # 프로덕션 빌드 (커밋 전 필수)
npm run db:push        # Supabase 마이그레이션 적용
npm run db:types       # DB 타입 생성

npx supabase link --project-ref <REF>
vercel deploy --prod
```

---

## 7. 현재 알려진 차단 요인

> 이 섹션은 해소될 때마다 갱신한다. 여기 있는 항목은 "코드 문제"가 아니라 "계정 문제"다.

- [ ] Supabase 프로젝트 미생성 → 여행 저장 불가 (현재 목 데이터로 동작)
- [ ] 네이버 클라우드 플랫폼 키 미발급 → 지도 렌더링 불가
- [ ] Google Maps API 키 미발급 → 지도 렌더링 불가
- [ ] Vercel CLI 미설치 (`npm i -g vercel`)
- [ ] 인증 미구현 → `trip` RLS가 `auth.uid()`를 요구하므로 저장 경로가 막혀 있음

---

## 8. 참고

- 전체 설계 근거: `README.md`
- 작업 분해: `wbs.md`
- DB 스키마: `supabase/migrations/`
